---
id: "006"
title: "实现 FastAPI 接口层与应用生命周期"
status: "done"
branch: "codex/t006-fastapi-api-lifecycle"
worktree: ".worktrees/t006"
created_at: "2026-03-10T10:20:03Z"
updated_at: "2026-03-10T11:42:05Z"
---

## 背景

前端和其他 agent 需要通过统一 REST API 访问截图、OCR、摘要与配置数据，且应用启动需同时管理 DB 与 Engine 生命周期。

## 目标

完成 `api/schemas.py`、`api/routes.py` 与 `app.py` 生命周期编排，提供文档约定的全部接口。

## 范围

- 包含：截图查询接口、图片文件接口、摘要查询与写入接口、配置查询与更新接口、启动/关闭钩子。
- 不包含：鉴权体系、外部多租户、复杂限流与审计系统。

## 实施计划（分步骤）

1. 设计 Pydantic schema 与参数校验规则，统一请求/响应结构。
2. 实现 `routes.py`，直接调用 `db/*` 模块完成 CRUD。
3. 在 `app.py` 中挂载路由并接入 DB 初始化、Engine 启停钩子。
4. 增加错误处理与返回码约定，保证接口行为稳定可预期。

## 验收标准

- 重构文档列出的 API 均可访问并返回正确结构。
- `GET /api/screenshots/{id}/image` 可返回图片二进制内容。
- 启动时可完成 DB 初始化并拉起 Engine，关闭时可安全释放任务。
- 接口异常场景有明确错误码与错误信息。

## 测试要求

- 使用 FastAPI TestClient 编写接口集成测试，覆盖所有路由。
- 覆盖参数校验失败、记录不存在、文件缺失等错误路径。
- 增加生命周期测试，验证启动/关闭钩子执行顺序与幂等性。


## 交付记录

- 完成时间：2026-03-10T11:42:05Z
- 分支：codex/t006-fastapi-api-lifecycle
- 实现提交：4d57be58a7dba58d0b8ad0e2e54bc56bca167993
- 合并提交：ada47e457bdc4d899de44d1e9f7b287218d77ea5
- 测试结果：
  - `python -m pytest -q recall/tests`（26 passed）
  - `python -m pytest -q recall/tests/test_api.py`（4 passed）
- 变更文件摘要：
  - `recall/api/schemas.py`：补充请求/响应 schema，增加 ISO8601 与时间范围校验。
  - `recall/api/routes.py`：实现 API 路由、统一异常返回、图片路径安全校验与 201 创建返回。
  - `recall/app.py`：完善 lifespan 生命周期，支持依赖注入并确保启动/关闭钩子幂等执行。
  - `recall/tests/test_api.py`：新增 API 与生命周期集成测试，覆盖成功与错误路径。
  - `recall/requirements.txt`：补充 `httpx` 测试依赖。
  - `doc/notice.md`：记录 TestClient 缺失 `httpx` 的环境问题与解决方案。
