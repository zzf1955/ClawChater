---
id: "001"
title: "搭建 recall 新工程骨架与基础配置"
status: "done"
branch: "codex/t001-recall-scaffold"
worktree: ".worktrees/t001"
created_at: "2026-03-10T10:20:03Z"
updated_at: "2026-03-10T10:52:40Z"
---

## 背景

`doc/重构.md` 已明确新架构与技术栈，但仓库当前只有 `recall_old`，缺少新的 `recall/` 工程骨架与统一配置入口。

## 目标

落地新的目录结构、基础配置与启动骨架，为后续数据库、服务编排、API 和前端开发提供稳定起点。

## 范围

- 包含：`recall/` 目录与子模块骨架、`app.py`/`config.py` 基础实现、前后端基础依赖与启动脚本、`data/` 路径约定。
- 不包含：业务功能实现（截图/OCR/API 细节）、页面业务交互、旧代码迁移。

## 实施计划（分步骤）

1. 按重构文档创建后端目录与空模块文件，保证包结构可导入。
2. 初始化 FastAPI 入口与配置模块，统一管理数据库路径、截图目录、运行参数。
3. 初始化 React + Vite + Tailwind 前端骨架并接入基础开发脚本。
4. 增加本地开发启动说明与最小自检命令，保证团队可快速运行。

## 验收标准

- `recall/` 目录结构与 `doc/重构.md` 规划一致。
- 后端服务可启动并返回健康检查响应。
- 前端项目可启动并构建成功。
- `data/` 相关路径可在首次运行时自动创建。

## 测试要求

- 后端：执行基础启动测试（如 `pytest` smoke 或 `uvicorn` 启动校验）。
- 前端：执行一次构建校验（如 `npm run build`）。
- 目录与配置：新增最小化单元测试，覆盖关键路径解析逻辑。

## 交付记录

- 提交信息：未提交（工作区仍有并行重构改动，未做自动提交/合并）
- 测试结果：
  - `python -m pytest recall/tests/test_config.py` 通过（2 passed）
  - `python -m compileall recall` 通过
  - `npm run build` 通过
- 变更文件摘要：
  - 新增 `recall/` 新工程骨架（FastAPI 入口、配置、db/api/services 基础模块）
  - 新增 `recall/frontend/` React + Vite + Tailwind 基础工程
  - 新增 `recall/tests/test_config.py` 路径与目录初始化测试
  - 更新 `doc/environment.md` 记录 Conda 与前后端基础环境
