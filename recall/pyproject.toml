[project]
name = "recall"
version = "0.1.0"
requires-python = ">=3.11"

authors = [{ name = "zzf621" }]

dependencies = [
  "fastapi==0.117.1",
  "uvicorn==0.36.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".."]
